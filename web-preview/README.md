Web preview for WalkToStopAlarm

Files:
- index.html — the preview page
- styles.css — basic styling
- script.js — small interaction (set default time and schedule button)

To run locally (requires Python 3):

```powershell
# from project root
cd web-preview
python -m http.server 8000
# open http://localhost:8000 in your browser
```
